# ==============================================
# EASYPARK PLATFORM - PRODUCTION CONFIGURATION
# ==============================================
# Configuration for Clever Cloud MySQL

# ==============================================
# DATABASE CONFIGURATION - Using Environment Variables
# ==============================================
spring.datasource.url=${DATABASE_URL:jdbc:mysql://bighqeizhtcbg2szlczt-mysql.services.clever-cloud.com:3306/bighqeizhtcbg2szlczt?useSSL=true&requireSSL=true&serverTimezone=UTC}
spring.datasource.username=${DATABASE_USERNAME:ues1f415lj3l7lcd}
spring.datasource.password=${DATABASE_PASSWORD:UDYMRQudWFk8FwJL25WF}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ==============================================
# JPA/HIBERNATE CONFIGURATION - PRODUCTION
# ==============================================
# update will automatically create tables in the database
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==============================================
# SERVER CONFIGURATION
# ==============================================
server.port=${PORT:8080}

# ==============================================
# AUTHORIZATION & SECURITY
# ==============================================
authorization.jwt.secret=${JWT_SECRET:your-secret-key-change-this-in-production-at-least-256-bits-long-for-jwt-token-signing}
authorization.jwt.expiration.days=7

# ==============================================
# CONNECTION POOL CONFIGURATION
# ==============================================
# Clever Cloud free tier allows max 5 connections - using only 1 temporarily
spring.datasource.hikari.maximum-pool-size=1
spring.datasource.hikari.minimum-idle=0
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=60000
spring.datasource.hikari.max-lifetime=120000
spring.datasource.hikari.leak-detection-threshold=20000
spring.datasource.hikari.auto-commit=true
spring.datasource.hikari.pool-name=EasyParkHikariPool
spring.datasource.hikari.register-mbeans=false
# Force close connections on shutdown
spring.datasource.hikari.allow-pool-suspension=true

# ==============================================
# LOGGING CONFIGURATION - PRODUCTION
# ==============================================
logging.level.org.springframework.boot.autoconfigure=INFO
logging.level.org.hibernate=WARN
logging.level.com.zaxxer.hikari=INFO

